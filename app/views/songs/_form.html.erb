<%= render 'application/errors' %>
<%= form_for @new_song do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :lyrics %>
  <%= f.text_field :body %>

  <%= f.submit 'Save Song', class: 'button' %>
<% end %>
