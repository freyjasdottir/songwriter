<div class='song-index'>
  <h2>Your Songs</h2>
  <ul>
    <% @user_songs.each do |song| %>
      <li class='song-title'><%= link_to song.title, song_path(song) %></li>
    <% end %>
  </ul>

  <%= link_to 'Add New Song', new_song_path, class: "button" %>
</div>
